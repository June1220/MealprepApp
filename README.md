# MealprepApp

Simple starter web app for busy working parents to generate a healthy weekly family meal-prep plan.

## Run locally

```bash
python3 -m http.server 8000
```

Then open `http://localhost:8000`.
